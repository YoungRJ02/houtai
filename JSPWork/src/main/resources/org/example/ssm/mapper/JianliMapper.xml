<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.ssm.mapper.JianliMapper">

    <!--Jianli find(Jianli jianli);-->
    <select id="find" resultType="Jianli">
        select * from jianli where Jname = #{Jname};
    </select>
</mapper>